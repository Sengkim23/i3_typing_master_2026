# i3_typing_master_2026

# Compile it
- Using VSCode's built-in run
- Using the command example:
```bash
javac welcome_menu.java
```

# Run it
- Using VSCode's built-in run
- Using the command example:
```bash
java welcome_menu
